# Frontend config. No secrets here — VITE_* vars are bundled into the browser.
# The API base URL defaults to http://localhost:3000 in code, matching the
# backend's default port. Only set VITE_API_BASE_URL to override locally.
VITE_ENABLE_INVENTORY_MODULES=true